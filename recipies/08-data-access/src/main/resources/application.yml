spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/customers
    username: customers
    password: customers
  flyway:
    baseline-on-migrate: true
  jpa:
    show-sql: true

server:
  port: 8100
